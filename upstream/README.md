# Upstream Repositories

This directory is intentionally used for cloned third-party repositories.

The root workspace tracks scripts and planning documents, while the cloned source trees under this directory remain separate working repositories.

Use `../scripts/bootstrap_upstreams.sh` from this directory, or `./scripts/bootstrap_upstreams.sh` from the workspace root, to populate it.

